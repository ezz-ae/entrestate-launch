'use client';

import { ColdCallingBoard } from '@/components/leads/cold-calling-board';

export default function ColdCallingPage() {
  return (
    <div className="animate-in fade-in duration-700">
      <ColdCallingBoard />
    </div>
  );
}
